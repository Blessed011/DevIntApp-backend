<html lang="en">

<head>
    <link href="/styles/index.css" rel="stylesheet" type="text/css">
    <link href="/styles/product-card.css" rel="stylesheet" type="text/css">
</head>

<header>
    <div class="search">
        <form action="/search" method="get">
            <input value="{{.searchName}}" class="search-input" type="text" name="moduleName" placeholder="Поиск...">
            <input type="submit" class="submit-button" value="Найти">
        </form>
    </div>
</header>

<body>
    <div class="many_cards">
        {{ range $index, $card := .modules }}
            {{template "product-card.tmpl" .}}
        {{ end }}
    </div>
</body>
</html>